This directory stores trained model files (.pkl).

Files expected here (generated in Phase 3):
  - diabetes_model.pkl   (RandomForest trained on Pima Indians dataset)
  - heart_model.pkl      (GradientBoosting trained on UCI Heart Disease dataset)

Until Phase 3 training is complete, the backend uses heuristic fallback predictions.
